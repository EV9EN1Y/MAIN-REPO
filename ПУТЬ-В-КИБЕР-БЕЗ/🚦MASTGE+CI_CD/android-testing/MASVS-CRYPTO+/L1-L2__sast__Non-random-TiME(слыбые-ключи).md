https://mas.owasp.org/MASTG/tests/android/MASVS-CRYPTO/MASTG-TEST-0205/
# MASTG-TEST-0205: Non-random Sources Usage

Тест проверяет, не использует ли приложение значения, которые выглядят случайными, но на самом деле легко предсказуемы (например, текущее время в миллисекундах, миллисекунды календаря), для генерации паролей, токенов, ключей или других важных данных. Эти значения могут быть предсказаны злоумышленником, что делает защиту бесполезной

## бывает такое, что могуг для генерации случайности используют текущее время! что безусловно, уникально, но позволяет вычислить значения токенов и наперед и в прошлом - небезопасно!

-----

через JADX
### чек-лист для поиска

**1. "Классические" источники времени**  
Это самые прямые и очевидные признаки, с которых обычно начинают.

- `System.currentTimeMillis()` — стандартный способ получить текущее время в миллисекундах
    
- `Date().getTime()` — делает то же самое, но через объект `Date
    
- `Calendar.getInstance().getTimeInMillis()` — еще один способ получить время через календарь
- `Calendar.MILLISECOND` — обращение к конкретному полю календаря[](https://mas.owasp.org/MASTG/tests/android/MASVS-CRYPTO/MASTG-TEST-0205/)[](https://mas.owasp.ac.cn/MASTG/tests/android/MASVS-CRYPTO/MASTG-TEST-0205/).
    

**2. "Хитрые" и непрямые пути**  
Здесь важно смотреть на контекст, так как код может использовать время, не вызывая эти методы напрямую.

- **`System.nanoTime()`**: Дает время с наносекундной точностью, но _не_ является источником энтропии и подходит только для измерения интервалов. Использовать его для генерации "случайных" значений - плохая идея.
    
- **`Calendar` без явного вызова `getTimeInMillis()`**: Например, `Calendar.getInstance().get(Calendar.SECOND)` или `get(Calendar.MILLISECOND)` — это тоже неслучайные значения.
    
- **`new Date()`**: Сам по себе объект `Date` содержит время создания.
    
- **`LocalDateTime.now()` (Java 8+)** - тоже берет время из системы.
    

.==`SecureRandom` 
Это самый коварный вариант. Искать нужно не просто вызов `System.currentTimeMillis()`, а ситуацию, когда это значение используется как **зерно (seed)** для генератора случайных чисел. Это полностью убивает криптостойкость.==

- **Ключевая сигнатура для поиска**: `SecureRandom.setSeed(System.currentTimeMillis())`
   
- Прямое подтверждение из Google: это опасная практика, так как предсказуемое значение делает сгенерированную последовательность уязвимой для атак



------

#### результат проверки через jadx от 03 07 2026

==SystemcurrentTimeMillis== используется во многих местах приложения но нигде не используется как seed для генерации ключей паролей токенов или других криптографически значимых значений

список использования currentTimeMillis

в ChatViewModel для генерации msgId сообщения m + currentTimeMillis это просто уникальный идентификатор а не токен или ключ
в ChatViewModel как timestamp сообщения время отправки
в SettingScreen для имени файла экспорта данных и timestamp в фидбеке
в ChatsScreen для имени сохраняемого видео и аудио файлов
в ListAllMyChatsScreen для вычисления разницы времени меньше ли 24 часов
в AudioRecorderBar для имени аудиофайла
в VideoProcessor для имени видеофайла
в CameraXRecorder для времени начала записи
в PinSessionManager для времени блокировки и проверки истекла ли блокировка
в CloudFunctionService для кэширования jwt проверки срока действия токена
в NotificationHelper для генерации notificationId chatId + currentTimeMillis hashcode
в FirebaseService для created_at timestamp
в MeetWayMessagingService для notificationId int currentTimeMillis
в MeetWayApp для измерения времени запуска приложения

date не используется new Date только для форматирования дат для ui и логов через SimpleDateFormat
CalendargetInstance не используется в коде приложения только иконки календаря из compose material
SystemnanoTime не используется в коде приложения
LocalDateTimenow не используется в коде приложения
SecureRandoms etSeed не используется в коде приложения

### итог ====== тест пройден 

приложение не использует предсказуемые временные значения для генерации ключей паролей токенов или других sensitive данных все использования currentTimeMillis носят служебный и некриптографический характер
