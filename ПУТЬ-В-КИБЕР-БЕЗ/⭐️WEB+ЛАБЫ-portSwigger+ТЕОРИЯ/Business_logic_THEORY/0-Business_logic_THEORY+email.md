
надо придумывать как обмануть логику работы сайта. например, применить купон сто раз или перевести минус денег

### Что такое уязвимости бизнес-логики?

Это не баги в коде вроде SQL-инъекций, а ошибки в логике работы приложения. Разработчики что-то не учли, и злоумышленник может заставить сайт работать не так, как задумано, но так, как написано. Проще говоря, это обманывание здравого смысла приложения

они возникают из-за неверных предположений о том, как пользователь будет себя вести. Например:

- разработчик думает: "Пользователь будет вводить цену в поле, и она будет положительной". А пользователь вводит отрицательную цену и получает деньги
    
- разработчик: "Пользователь пойдет по шагам А → Б → В". А хакер идет сразу на шаг В, минуя оплату
    
- разработчик думает: "На купоны можно нажать один раз". А хакер нажимает 100 раз
    

> Такие уязвимости уникальны для каждого сайта, их сложно найти автоматическими сканерами, потому что сканер не понимает, как должна работать логика магазина, форума или банка.

###  примеры из реальной жизни (и лаб)

1. изменение цены в запросе / добавляешь товар в корзину, перехватываешь запрос и меняешь `price=1000` на `price=1`. Сервер доверяет клиенту
    
2. Отрицательное количество Пытаешься заказать «минус 5» товаров. Если сервер считает баланс как `цена * количество`, можно уйти в минус и обнулить счет
    
3. **Манипуляции с купонами**. Использовать купон много раз, или применить два разных купона, которые не должны суммироваться
    
4. **Обход рабочего процесса**. Например, сайт доставки еды: шаг 1 — выбор ресторана, шаг 2 — выбор блюд, шаг 3 — оплата. Если перейти сразу на шаг 3, подставив ID ресторана и блюда из запроса, можно заказать еду бесплатно, минуя оплату
    
5. **Недостаточная валидация ввода**. В поле «Имя» при регистрации можно ввести HTML-теги или эмодзи, которые потом сломают верстку личного кабинета администратора
    

###  Как защищаться

- не доверяй клиенту**. Никогда. Все критичные данные (цены, количество, скидки) должны храниться и проверяться на сервере
    
- понимай свой бизнес**. Разработчики и тестировщики должны знать, как работает бизнес на уровне предметной области. Только тогда они заметят, что кто-то пытается заказать товар с отрицательной ценой
    
- описывай логику**. Нужны понятные схемы и документы, описывающие все возможные сценарии: что, если пользователь нажмет «Назад» в браузере во время оплаты? Что, если он откроет две вкладки с одной корзиной?
    
- пиши понятный код**. Код должен быть чистым, чтобы другой разработчик сразу понял предполагаемую логику и заметил, где она может сломаться
    
- проверяй крайние случаи**. Нулевые значения, отрицательные числа, пустые строки, максимально длинные строки — все это нужно тестировать
    
- думай как злоумышленник**. На этапе тестирования нужно задавать вопрос: «Как эту функцию можно использовать не по назначению?»




--------

### основные типы логических уязвимостей (с примерами)

#### излишнее доверие к клиентской части

разработчики думают, что раз пользователь работает через браузер, то и данные будут приходить только оттуда, а значит, клиентской валидации достаточно. это ловушка. атакер всегда может перехватить запрос (через бурп) и изменить данные уже после того, как браузер их отправил, но до того, как они ушли на сервер. вся клиентская защита становится бесполезной. пример: ты видишь в html скрытое поле с ценой товара и меняешь её перед отправкой.

#### игнорирование нестандартного ввода

приложения часто проверяют, например, что количество товара не превышает остаток на складе. но забывают проверить, что оно не отрицательное. если в форме перевода денег сумма не проверяется на отрицательность, можно отправить `-1000$` и получить деньги на свой счёт вместо отправки. всегда пробуй вводить запредельно большие числа, отрицательные, очень длинные строки, пустые значения или вообще удалять параметры

#### ошибочные предположения о поведении пользователя = это большая группа, вот основные подтипы:

- _доверие к "проверенным" пользователям._ систему могут жестко контролировать при первом действии (например, при регистрации), но потом, решив что пользователь уже свой, перестают проверять. если после регистрации с подтвержденным email дать возможность менять email без новой проверки, можно заменить его на чужой и украсть аккаунт
   
- _пользователь заполнит все поля._ разработчики думают, что раз поле обязательное в форме, то браузер не даст его пропустить. атакер просто удаляет параметр из запроса целиком. это может открыть доступ к другому коду в том же скрипте, который выполняется, если параметра нет. всегда пробуй удалять по одному параметру из запроса (и имя, и значение) и смотришь, что будет
   
- _пользователь пройдет все шаги по порядку._ сайт ждет: страница логина → страница с двухфакторкой → личный кабинет. атакер переходит сразу на третий шаг. если на сервере не проверяется, что пользователь действительно прошел второй шаг, двухфакторка не сработает. пробуй перескакивать через шаги, возвращаться назад, повторять один шаг много раз
   

#### ошибки, специфичные для предметной области

тут нужно знать, как работает конкретный бизнес. 
классика — скидки и купоны в интернет-магазинах. пример: скидка 10% на заказ от 1000$ . можно положить товаров на 1000$ ,  получить скидку, а потом удалить лишние товары из корзины перед оплатой. в итоге заказ будет на сумму меньше 1000$, но со скидкой, потому что логика её применения сработала раньше, чем удаление товаров

#### предоставление "оракула шифрования"
если приложение позволяет пользователю что-то зашифровать и выдает результат (например, в виде куки или параметра), а потом где-то ждет на вход такие же зашифрованные данные, это беда. атакер может зашифровать через этот "оракул" всё что угодно и подставить в другое, более чувствительное место. особенно опасно, если есть и "обратный оракул", который расшифровывает — тогда можно изучить структуру данных

## как искать (методология)



1. **изучать функционал.** не просто смотри, а пытайся понять, как он спроектирован. какие правила бизнеса он должен реализовывать?
   
2. **провоцировать ошибки.** посылай то, чего разработчик не ждет: странные числа (отрицательные, огромные), удаляй параметры, нарушай порядок шагов.
   
3. **читать ошибки.** если приложение упало и выдало стектрейс или сообщение — это золотая жила. оттуда можно узнать про внутреннюю структуру, имена файлов, запросы к базе.
   
4. **комбинировать функции.** подумай, как можно использовать две обычные функции вместе, чтобы получить необычный результат. например, функцию "применить купон" и функцию "удалить товар".
   
5. **изучать документацию.** если сайт на непонятную тему (например, биржевой софт), читай мануалы или спрашивай знающих людей. иначе просто не поймешь, что тут можно сломать.
   
-------

## расхождение в парсинге (parser discrepancy)
## ниже речь о том, как выполнить регистрацию например так, чтобы зарегать один почтовый адресс, а письмо получить на дргуой почтовый адресс



#### краткая теория по статье:

https://portswigger.net/research/splitting-the-email-atom


###  Основная идея

Некоторые сайты используют email для определения принадлежности пользователя к организации (например, по домену `@company.com`). Исследователь Гарет Хейз нашел способы заставить разные системы (сайт, библиотеки парсинга, почтовый сервер) по-разному понимать один и тот же email. 

>	Это позволяет обмануть контроль доступа, отправив письмо на один домен, а в системе оно будет выглядеть как принадлежащее другому

### Ключевые техники обмана парсеров

1. **UUCP и source routes**. Использование архаичных протоколов. Например, адрес `user@example.com!collab\@target.com` может быть интерпретирован так, что письмо уйдет на `collab@example.com`, а не на `target.com`
    
2. **Юникодные переполнения**. Если система неправильно обрабатывает юникод, символ вроде `❀` (код 10048) при определенных операциях может преобразоваться в `@`. Это позволяет обойти блокировки нежелательных символов
    
3. **Encoded-word (RFC2047)**. Это встроенная система кодирования в email, позволяющая использовать последовательности вроде `=?UTF-8?Q?=41=42=43?=@domain.com`, которые декодируются в `ABC@domain.com`. Можно создавать многослойные вложения (например, base64 + UTF-7), чтобы получить нужный адрес после всех преобразований
    

### Пример из исследования: уязвимость в GitHub

GitHub использовал Ruby-библиотеку `Mail`, поддерживающую эту кодировку. С помощью специально сформированного email исследователь смог заставить парсер библиотеки и почтовый сервер интерпретировать адрес по-разному. Это позволило обойти защиту Cloudflare "Zero Trust" и получить доступ к внутренним сетям через SSO, основанный на email-домене

### Главный вывод

Email — это сложный формат с 50-летней историей. Полагаться на парсинг email-адресов для принятия решений по безопасности (особенно для контроля доступа или аутентификации) очень опасно. Разные системы (валидаторы, библиотеки, почтовые серверы) могут понять один и тот же адрес по-разному, что создает почву для обхода защиты


----------

-------

способы коротко

`oastify.com!collab\@example.com`  ! и \

`collab%psres.net(@example.com`  % вместо @ и (

`❀`, вместо `@`    меняем собачку юникодом

`=?UTF-8?Q?=41=42=43?=@psres.net` декодируется в `ABC@psres.net`  (hex или base64)

`"external@evil.com"@company.com`  ошибка в библиотеке nodemailer

`email.parser`  это багованный метод в питоне

unicode soft hyphen (`U+00AD`) "p-a-s-s-w-o-r-d" — бессмыслица. для пользователя в почтовом клиенте отображается как обычное "password"

------------


### примеры
#### Exploiting parsers (Splitting the email atom)

это база, исследование гareth heyes с black hat 

##### пример 1: uucp протокол

- **описание:** использование восклицательного знака — пережитка эпохи uucp (unix to unix copy). он меняет порядок доменов
   
- **пример:** `oastify.com!collab\@example.com`. sendmail воспримет это так, что письмо нужно отправить на `collab@oastify.com`, а не на [example.com]. обратный слэш экранирует `@`
   
##### пример 2: source routes и процент

- **описание:** для postfix нашлась вариация с процентом и скобками. это аналог "percent hack", где `%` заменяется на `@`
   
- **пример:** `collab%psres.net(@example.com`. postfix проигнорирует домен в скобках и отправит письмо на `collab@psres.net`
   
##### пример 3: юникодные переполнения

- **описание:** генерация заблокированных символов (например, `@`) через переполнение юникода. php функция chr() берет код символа по модулю 256
   
- **пример:** символ `❀` имеет код 10048. `10048 % 256 = 64` (код `@`). если система пропускает `❀`, но блокирует `@`, это можно обойти
   
##### пример 4: encoded-word (rfc2047)

- **описание:** встроенная в email система кодирования. позволяет использовать hex или base64 прямо в адресе
   
- **пример:** `=?UTF-8?Q?=41=42=43?=@psres.net` декодируется в `ABC@psres.net`. исследователь нашел это в ruby gem 'mail', что позволило атаковать github
   
### Реальные баги и CVE

##### пример 5: cve-2025-13033 (nodemailer)

- **описание:** уязвимость в библиотеке nodemailer. неправильная обработка кавычек в локальной части адреса позволяет подменить получателя 
   
- **пример:** если система ожидает письмо для `internal@company.com`, можно сконструировать адрес вроде `"external@evil.com"@company.com`. из-за ошибки парсинга письмо уйдет на `external@evil.com`
   
##### пример 6: баг в парсере python

- **описание:** проблема в модуле `email.parser` python. неправильное декодирование преамбулы в multipart письмах приводит к появлению "суррогатных" символов (ошибки юникода) 
   
- **применение:** хотя это больше про краш, такие штуки показывают, насколько хрупок парсинг и как легко сломать логику
   
###  Access control bypass

##### пример 7: фишинг с невидимыми символами

- **описание:** киберпреступники используют unicode soft hyphen (`U+00AD`) в теме письма, чтобы сломать сигнатуры спам-фильтров 
   
- **пример:** слово "password" кодируется так, что между буквами вставляются невидимые символы. для фильтра это "p-a-s-s-w-o-r-d" — бессмыслица. для пользователя в почтовом клиенте отображается как обычное "password". фильтр пропускает, пользователь видит легитимное письмо




-----
-------

----------


### вот тут пооезная таблица с готовыми  древними кодировками IMAP UTF-7 (RFC 3501) 

### 1. (управляющие и специальные)

|Символ|Назначение|Кодировка в IMAP UTF-7|
|---|---|---|
|**`&`**|Сам амперсанд (нужен как текст)|**`&-`**|
|**`+`**|Плюс (в IMAP версии не управляющий, но может влиять)|`+` (остается как есть)|
|**`-`**|Минус (закрывает блок)|`-` (остается, если не внутри блока)|

### 2. КЛЮЧЕВЫЕ ДЛЯ АТАК 

|Символ|Unicode|Base64 (UTF-16BE)|Кодировка|Где применяется|
|---|---|---|---|---|
|**`@`**|U+0040|AEA=|**`&AEA-`**|Разделитель user/domain, основа обмана|
|**`:`**|U+003A|ADA=|**`&ADA-`**|Source routes (маршрутизация почты)|
|**`,`**|U+002C|ACM=|**`&ACM-`**|Разделитель в source routes|
|**`%`**|U+0025|ACU=|**`&ACU-`**|Percent hack (замена на @ в Postfix)|
|**`!`**|U+0021|ACE=|**`&ACE-`**|UUCP (восклицательный знак)|
|**`\`**|U+005C|AFw=|**`&AFw-`**|Экранирование (как в вашем примере с обратным слешем)|

### 3. ПОЛЕЗНЫЕ ДЛЯ ОБХОДА ФИЛЬТРОВ

|Символ|Unicode|Base64 (UTF-16BE)|Кодировка|Зачем|
|---|---|---|---|---|
|**`.`**|U+002E|AG4=|**`&AG4-`**|Точка в домене (если фильтр тупит)|
|**`/`**|U+002F|AG8=|**`&AG8-`**|Слеш (пути, редко)|
|**`#`**|U+0023|ACM= (ой, это же %? Проверим)|**`&ACM-`**|Хеш (реже, но бывает)|
|**`$`**|U+0024|ACQ=|**`&ACQ-`**|Доллар (ценники)|
|**`*`**|U+002A|ACg=|**`&ACg-`**|Звездочка (wildcard)|
|**`?`**|U+003F|AD8=|**`&AD8-`**|Вопрос (параметры)|

### 4. КИРИЛЛИЦА (если нужно подделать русский домен/имя)

|Символ|Unicode|Base64 (UTF-16BE)|Кодировка|
|---|---|---|---|
|**а**|U+0430|BDA=|**`&BDA-`**|
|**б**|U+0431|BDE=|**`&BDE-`**|
|**в**|U+0432|BDI=|**`&BDI-`**|
|**г**|U+0433|BDM=|**`&BDM-`**|
|... (дальше по алфавиту, но принцип ясен)||||

### 5. ПРОБЕЛ И ДРУГИЕ НЕВИДИМЫЕ

|Символ|Unicode|Base64 (UTF-16BE)|Кодировка|
|---|---|---|---|
|пробел|U+0020|ACA=|**`&ACA-`**|
|табуляция|U+0009|AAk=|**`&AAk-`**|
|перевод строки|U+000A|AAo=|**`&AAo-`**|



или вручную вот так можно кодировать 
```python
// Функция для получения IMAP UTF-7 одного символа
function getImapUtf7(char) {
    // Код символа в UTF-16BE (как 2 байта)
    let code = char.charCodeAt(0);
    // Старший и младший байт
    let highByte = (code >> 8) & 0xFF;
    let lowByte = code & 0xFF;
    
    // Превращаем в Base64 (имитация UTF-16BE)
    // На самом деле тут надо честное base64 от двух байт
    // Но для ASCII символов (код < 256) highByte = 0, lowByte = code
    // Тогда Base64 от [0, code] — это и есть наша таблица
    
    // Для не-ASCII придется честно кодировать, но для английских символов таблицы выше хватит
    console.log(`Символ: ${char} (U+${code.toString(16).toUpperCase()})`);
}
// Проверка
getImapUtf7('@'); // U+0040 → &AEA-
```








